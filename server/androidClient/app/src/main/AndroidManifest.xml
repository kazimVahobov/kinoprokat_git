<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kinoprokat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".common_activities.ThReportDetail"></activity>
        <activity
            android:name=".modules.theater.activities.TheaterReportList"
            android:label="@string/title_activity_theater_report_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".modules.theater.activities.TheaterMainPage" />
        <activity android:name=".common_activities.LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>